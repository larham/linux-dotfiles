# opencode (not tracked)
dot_config/opencode/

# nvim (not tracked)
dot_config/nvim/

# 1Password CLI (machine-specific, not tracked)
dot_config/op/
dot_config/private_op/

# macOS metadata
.DS_Store
**/.DS_Store
**/dot_DS_Store

# chezmoi age key (never commit)
key.txt

# yazi backup files
dot_config/yazi/yazi.toml-*

# git internals from submodules (e.g. superpowers/)
**/dot_git/
README.md

# Age-encrypted secrets (ignored if age identity key is missing)
{{- if not (stat (joinPath .chezmoi.homeDir ".config/chezmoi/key.txt")) }}
dot_config/gh/encrypted_private_hosts.yml.age
private_dot_ssh/encrypted_config.age
{{- end }}

